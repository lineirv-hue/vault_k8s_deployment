name: Deploy Vault Terraform

on:
  workflow_run:
    workflows:
      - CI
    branches:
      - main
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: [self-hosted, macos]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.6.5"

      - name: Cleanup existing Kubernetes resources
        run: |
          kubectl delete deployment vault --ignore-not-found --wait=false
          kubectl delete service vault --ignore-not-found --wait=false
          kubectl delete configmap vault-config vault-logrotate --ignore-not-found --wait=false
          kubectl delete pvc vault-data-pvc --ignore-not-found --wait=false
          kubectl patch pv vault-data-pv -p '{"metadata":{"finalizers":null}}' 2>/dev/null || true
          kubectl delete pv vault-data-pv --ignore-not-found --wait=false

      - name: Deploy Vault
        run: bash scripts/terraform-deploy.sh
